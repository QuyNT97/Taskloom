# @yuqgnort/taskloom-kernel

Policy-free domain and execution kernel for Task Engine. It contains immutable
state, transactions, plugin contracts, the dispatch pipeline and `TaskRuntime`.

Most applications should import `@yuqgnort/taskloom`. This package exists for
advanced integrations and policy packages that need the lowest-level public API.
