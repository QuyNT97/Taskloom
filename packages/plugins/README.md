# @yuqgnort/taskloom-plugins

Built-in Task Engine policies implemented entirely through the public kernel API:
FIFO, concurrency, priority, timeout, retry and latest-by-key.

Most applications can import these from `@yuqgnort/taskloom`. Import this package
directly when building a custom distribution on `@yuqgnort/taskloom-kernel`.
